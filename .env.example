# Copy to .env and adjust. Defaults to a local SQLite file if unset (see app/core/config.py) --
# that default exists only because no Postgres instance is available on the current dev machine.
# Postgres is the target dialect; set this before adding real tables in Sprint 0.2.
DATABASE_URL=postgresql+psycopg://user:password@localhost:5432/np_planning
